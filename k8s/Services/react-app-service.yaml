apiVersion: v1
kind: Service
metadata:
  name: react-app-service
spec:
  selector:
    app: react-app
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 80
      nodePort: 30001
